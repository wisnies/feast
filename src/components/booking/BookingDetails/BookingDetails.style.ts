import styled from 'styled-components';
import { StyledTitle, StyledText } from '@/styles/typography';

export const Container = styled.div``;

export const BDTitle = styled(StyledTitle)``;
export const BDText = styled(StyledText)``;
